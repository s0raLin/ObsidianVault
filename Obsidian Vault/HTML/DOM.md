DOM是将HTML表示为树状结构的模型
我们创建一个简单的文档:
```html
<!DOCTYPE html>
<html>
	<head>
		<title>DOM</title>
	</head>
	<body>
		<h1>What is DOM?</h1>
		<p>DOM is...</p>
	</body>
</html>
```

它可以表示为DOM树

![[DOM树.png]]

每个树节点都是HTML文档的元素